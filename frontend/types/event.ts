export interface CampusEvent {
    id: string;
    title: string;
    latitude: number;
    longitude: number;
    radius_meters: number;
    start_time: string;
    end_time: string;
  }

  // to be changed as needed!