name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npx jest --coverage

      - name: Upload coverage to Codecov (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'
          Invoke-WebRequest -UseBasicParsing -Uri https://cli.codecov.io/latest/windows/codecov.exe -OutFile codecov.exe
          .\codecov.exe upload-process --disable-search --fail-on-error -f coverage/lcov.info -F frontend
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload coverage to Codecov (Linux)
        if: runner.os == 'Linux'
        shell: bash
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          set -euo pipefail
          curl -fsSL https://cli.codecov.io/latest/linux/codecov -o codecov
          chmod +x codecov
          ./codecov upload-process --disable-search --fail-on-error -f coverage/lcov.info -F frontend


      - name: Generate Next.js types
        run: npx next typegen

      - name: Type check
        run: npm run typecheck --if-present

      - name: Lint
        run: npm run lint --if-present

      - name: Build
        run: npm run build

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npm test -- --coverage --runInBand
      
      - name: Upload coverage to Codecov (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'
          Invoke-WebRequest -UseBasicParsing -Uri https://cli.codecov.io/latest/windows/codecov.exe -OutFile codecov.exe
          .\codecov.exe upload-process --disable-search --fail-on-error -f coverage/lcov.info -F Backend
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload coverage to Codecov (Linux)
        if: runner.os == 'Linux'
        shell: bash
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          set -euo pipefail
          curl -fsSL https://cli.codecov.io/latest/linux/codecov -o codecov
          chmod +x codecov
          ./codecov upload-process --disable-search --fail-on-error -f coverage/lcov.info -F Backend


      - name: Type check
        run: npm run typecheck --if-present

      - name: Lint
        run: npm run lint --if-present

      - name: Build
        run: npm run build --if-present
